services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: skillora
      POSTGRES_USER: skillora
      POSTGRES_PASSWORD: skillora
    ports: ["5432:5432"]
    volumes: [postgres_data:/var/lib/postgresql/data]
volumes:
  postgres_data:
